# Optional — used to sign customer account session cookies.
# A safe built-in fallback is used if unset, so the site runs with zero env vars.
ACCOUNT_SESSION_SECRET=

# Optional — public site URL used for canonical / Open Graph tags.
NEXT_PUBLIC_SITE_URL=https://stratum3d.vercel.app
